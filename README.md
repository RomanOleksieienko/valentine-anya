# Valentine's Website for Anya ❤️

Этот проект — сайт-валентинка с милыми 8-битными анимациями.

## 🚀 Как обновлять сайт

Поскольку сайт развернут через Vercel и подключен к GitHub, любые изменения в коде автоматически обновят сайт.

### Как внести изменения (текст, картинки):

1.  **Отредактируйте файлы** на компьютере (например, `index.html` для текста или замените файлы картинок в папке).
2.  **Отправьте изменения** на GitHub с помощью этих команд в терминале (в папке проекта):

```bash
git add .
git commit -m "Описание изменений (например: поменял текст)"
git push
```

3.  **Готово!** Vercel увидит новый коммит и сам пересоберет сайт за минуту.

## 🖼 Как поменять финальное фото

1.  Найдите файл в проекте.
2.  Назовите ваше фото, например, `us.jpg`.
3.  Положите его в папку проекта.
4.  В файле `index.html` найдите строку:
    ```html
    <img src="https://via.placeholder.com/..." ... class="couple-photo">
    ```
    и замените `src` на ваше имя файла:
    ```html
    <img src="us.jpg" alt="Наше фото" class="couple-photo">
    ```
5.  Сделайте `git add .`, `git commit`, `git push`.

С днем Святого Валентина! 💘
